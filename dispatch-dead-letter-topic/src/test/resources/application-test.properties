# Sobreescribimos el servidor de Kafka
kafka.bootstrap-servers=${spring.embedded.kafka.brokers}

# Indicamos el puerto con el valor de OrderDispatchIntegrationTest con la anotación @AutoConfigureWireMock(port = 0)
dispatch.stockServiceEndpoint=http://localhost:${wiremock.server.port}/api/stock